---
description: shadcn ui/common, fonts deferred, toast/icons manual
globs: src/components/**/*.{ts,tsx}
alwaysApply: false
---

# Components

- shadcn CLI → `components/ui` (includes `cn` setup).
- Complex shared UI + `AppProviders` → `components/common`.
- Fonts **Inter** + **Vazirmatn**: apply only when explicitly requested after shadcn.
- Toasts: install manually when first needed (not shadcn CLI).
- Icons: install when needed.
- Prefer `next/image`, `next/font`, Next navigation. Dynamic-import heavy UIs.
- TipTap deferred until editor feature starts.
